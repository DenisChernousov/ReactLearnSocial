import React from 'react';
import s from './Header.module.css';
const Header = (props) => {
    return (
        <div className={s.items}>
         <p>Logo</p>
        </div>
    );
}

export default Header;